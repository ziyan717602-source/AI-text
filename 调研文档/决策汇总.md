***
我把你上传的调研汇总和你点名的几个仓库一起看完了。先给结论：

**最适合你的路线，不是“纯 RAG”，也不是“全靠一个超长 prompt 一次成文”，而是：`Markdown/文件系统为主的刚性知识库 + 渐进式披露 + 轻量多 Agent + 局部检索增强`。**
你上传的报告里最有价值的部分，正是把长篇小说看成一个“可编排、可校验、可回写”的工程系统，而不是聊天框里的一次性生成。报告明确强调了 Markdown 纯文本图谱、状态机编排、Schema 校验、以及用 Scout/单场景简报来防止 plot bleed；这和 craft-companion 的结构化知识库与 5 阶段流程、以及 ai_creator 的 `World -> Project -> Chapter` 分层，是同一路数。 ([GitHub][1])

但我会修正你报告里的一个过强判断：**“彻底摒弃 RAG”太绝对了。**
更准确的说法应该是：**不要把 RAG 当“小说真相库”，但可以把它当“辅助召回层”。** 你的 canonical truth 应该放在结构化 Markdown/YAML/JSON 状态里；而 RAG、FTS5、embedding、rerank 适合拿来召回参考资料、旧章节片段、相似场景和作者样文。稿匣就明确采用了 `SQLite / FTS5 + embedding + rerank` 的混合检索；AI_NovelGenerator 也把语义搜索和状态跟踪放进了长篇流程；最近的 TreeWriter、CogWriter、DOME、EIPE 这类研究，也都支持“分层规划 + 外部记忆/检索”优于单次长文本直写。 ([GitHub][2])

下面我按你真正关心的东西说。

## 一、这些技术路线各自值不值得借

### 1) ai_creator

它最值得借的是**内容层级设计和产品化架构**：`World -> Project(Novel) -> Chapter`，角色和世界设定放在 world 层，草稿可挂在 world 或 project；同时它还有语义搜索、CRUD、续写、概要生成、记忆等工具，并且用了共享 Zod schema、服务端/网页/移动端三端结构。这个仓库更像“写作平台底座”。弱点是社区规模目前不大，且公开信息里没有完整 README 暴露全部创作方法论，所以更适合拆架构，不适合直接照搬创作流。([GitHub][3])

### 2) inkos

它是你列的几个里**最接近“可运行的长篇多 Agent 写作流水线”**的：仓库公开描述了写、审、改的 Agent 思路，README 里也强调 human review gates；当前还有 Studio、TUI、本地工作台、AI 检测、文风分析等配套，且截至 2026-05-06 还有持续版本发布。它适合你研究“多 Agent 如何串起来”。弱点也很明显：复杂、重、token 花销高，而且 issue 活跃说明真实使用中还有不少工程问题。([GitHub][4])

### 3) craft-companion

这个仓库和你的目标**概念最贴脸**。它强调结构化知识库、双入口初始化、5 阶段工作流，并且明确提出“不要提前写正文”，先读 `START_HERE.md`、统一入口、模板，再进入创作；流程里还有 Writer、Evaluator、Arbiter、检查点、知识库更新。它特别适合你借鉴“渐进式披露”“场景切片投喂”“章节生成后回写知识库”这些关键设计。弱点是社区还小，成熟度和生态都不如 inkos。([GitHub][1])

### 4) 稿匣

稿匣是**最像你未来想做的工具产品雏形**的：本地优先桌面工作台、章节/设定/资料/历史统一放本机，支持导入 txt/md/json/csv/html/docx/pdf，知识检索是 `SQLite / FTS5 + embedding + rerank`，还有 Agent 执行轨迹、本地历史、章节写回。它非常适合你研究“本地优先 + 可审阅 + 可追溯”的工具设计。弱点是它目前还是公开预览版、星数很少，而且许可证是自定义许可，不适合作为你商用产品的直接底座。([GitHub][2])

### 5) AI_NovelGenerator

它的优点是**模块齐、GUI 明确、概念对长篇友好**：世界观、人物、情节蓝图、多阶段章节生成、状态跟踪、伏笔管理、语义搜索、自动校对、可视化工作台，都已经列出来了。问题在于：主分支作者自己说明过维护精力有限，而 dev 分支又处于重构期、尚未 feature-complete，所以更适合你拆模块思路，不适合当稳定基座。([GitHub][5])

---

## 二、对你四个核心疑问的直接回答

### 1. 小说信息管理：到底用 RAG，还是 `.md`？

我的答案是：

**用 `.md/.yaml/.json` 做“真相库”，用检索做“召回层”。**
不要二选一。

你最该避免的，不是“没用 RAG”，而是**把所有角色、设定、伏笔、真相都写进一张大角色卡，然后每次让模型整张吞进去**。这会同时导致两件事：
一是 prompt 爆炸；
二是你担心的“部分信息提前被披露”真的会发生。

正确做法是把一个角色拆成至少四层：

```text
characters/
  lin_qiu/
    public.md        # 任何出场场景都可见
    hidden_truth.md  # 身世真相/反转/后期秘密
    state.json       # 当前章结束时的动态状态
    voice.md         # 说话习惯、禁用口癖、行为偏好
```

再加上：

```text
timeline/
  events/
  unresolved_threads/
world/
  rules/
  factions/
outline/
  macro/
  arc/
  scene/
```

**模型运行时永远不要“读全库”，而是先由 Scout/Planner 组装一个 scene packet。**
你上传的报告里把这件事说得很对：用“侦察员/前置规划智能体”从宏观大纲切出一个去语境化的单场景简报，并对写手智能体实行渐进式披露，物理上阻断 plot bleed。craft-companion 的初始化和 5 阶段流程，本质上也是先结构化，再写正文。 ([GitHub][1])

所以，**不是“RAG 管理小说”**，而是：

* `.md/.yaml/.json` 管理**确定事实**
* `FTS/embedding/rerank` 检索**可能相关的旧片段与参考资料**
* `scene packet` 决定**这一场该给 Writer 看什么**

一句话概括：**RAG 用来找，Markdown 用来定。**

---

### 2. 如何解决 AI 味过重：分层 skills 这条路对不对？

**对，但不能分得过细。**

你说的“写人/写事/写景 → 具体人/具体事/具体景 → 具体写法”的思路，本质上是把 prompt 从“堆要求”改成“分层约束”。这条路和 TreeWriter、CogWriter 这类“分层规划优于单次长文直出”的研究方向是一致的。([arXiv][6])

但它有三个容易踩的坑：

1. **层太多，编排成本爆炸**
   skill 一多，调度就会很重，互相打架，还会让 token 消耗飙升。

2. **层太细，文风会被“模板化”**
   如果每一段都套“写景 skill”“对白 skill”“情绪 skill”，最后不是更像人，而是更像流水线。

3. **后置润色会冲掉前面细节**
   很多“去 AI 味” agent 会把原本具体的动作、节奏、口气，改成更顺但更空的句子。

我建议你把 skills 只保留成三层：

**第一层：硬约束层**
全书通用，不变。比如禁用词、叙述人称、节奏底线、文风禁区、不要总结升华。

**第二层：场景层**
按场景类型给规则：对话场、冲突场、调查场、暧昧场、回忆场、写景场。
每次只启用一个主 skill，最多加一个副 skill。

**第三层：技法层**
只做局部增强，比如“压缩解释量”“对白去书面腔”“动作描写落到肢体细节”“减少抽象形容词”。

不要再往下拆了。
**真正去 AI 味，不靠“多技能”，而靠“少量硬禁令 + 场景专用 skill + 人类样文对齐 + 局部重写”。**

---

### 3. 写作流程：一次成文，还是写作 Agent + 润色 Agent？

我的建议很明确：

**不要纯一次成文，也不要搞一个会重写全章的“大润色 agent”。**
最稳的是一个**轻量四段式**：

1. **Scout / Planner**
   输入细纲、角色状态、当前章目标，输出 scene packet

2. **Writer**
   只负责把 packet 写成初稿

3. **Auditor**
   只检查四件事：设定冲突、时间线冲突、提前披露、AI 味/口吻漂移

4. **Patch Rewriter**
   只改被 Auditor 标红的段落，不全章洗稿

这个思路比“一次成文”稳，也比“全章润色 agent 二次重写”更保留原稿的锐度。inkos 的“写、审、改”门控很适合借鉴，craft-companion 的“章纲→初稿→自查→修订→终版确认”也说明长篇协作更适合分阶段，而不是一把梭。([GitHub][7])

至于 token 成本，你上传的报告里给出的结论是：分离式多 Agent 流程每章大约可能是一次成文的两倍左右，这个方向判断是合理的；但那组数字本身是估算，而且报告明确写了“AI 生成”，所以你应把它当**量级参考**，不要当 benchmark。

我的实战建议是：

* **开篇、高潮、关键反转章**：走四段式
* **过桥章、交代章、常规推进章**：走两段式（Planner → Writer）
* **只对被审计命中的段落做 patch rewrite**
* **不要做全章“润色美化”**

因为**全章润色最容易把“去 AI 味”做成“统一抹平”**。

---

### 4. 路线验证：先拿一段细纲试跑，再迭代，合不合适？

**非常合适，但还不够工程化。**

你现在的想法是对的：先找一段细纲或已有小说片段，建立初版流程，跑到效果可接受，再继续补功能。
我建议你把它升级成一个**固定 benchmark harness**，不要只靠“读起来顺不顺”。

最小验证集建议这样做：

**A 组：单场景测试**

* 对话场
* 动作冲突场
* 情绪/暧昧场
* 信息揭示场
* 写景/氛围场

**B 组：单章测试**

* 1 章 2500~4000 字，有明确起承转合

**C 组：迷你长篇测试**

* 连续 5 章，共享同一批角色状态和伏笔

然后每次对比三条路线：

* Route A：一次成文
* Route B：Planner → Writer
* Route C：Planner → Writer → Auditor → Patch

评估不要只看“好不好看”，至少看这 6 项：

1. 设定冲突数
2. 时间线冲突数
3. plot bleed 次数
4. 对话声纹可区分度
5. AI 味黑词/黑句式密度
6. 人类盲测偏好

TreeWriter 的研究结论之一，就是层级化组织和上下文动态加载，会明显提高作者控制感与 AI 帮助度；这正说明你应该先测流程，而不是先堆功能。([arXiv][6])

---

## 三、我给你的最终技术路线

### 结论版

**你最该做的是：**

* 用 **Obsidian/本地目录** 当作者工作区
* 用 **Markdown + YAML/JSON** 当真相库
* 用 **Codex/CC** 当编排器、脚手架、文件读写代理
* 用 **FTS5/embedding/rerank** 做辅助召回
* 用 **状态机工作流** 代替“一个大 prompt”
* 用 **局部审计 + 局部改写** 代替“全章润色”

### 不建议的路线

* 纯 RAG 托管全部角色/情节真相
* Writer 直接读整本设定库
* 一个总 prompt 里塞世界观、所有角色、所有要求
* 生成后再让“润色 agent”整章重写
* 一上来就造复杂图数据库和超多子 Agent

### 推荐的 MVP 架构

```text
novel-project/
  00_rules/
    global_style.md
    forbidden_patterns.md
  01_world/
    rules.md
    factions.md
    geography.md
  02_characters/
    <char>/public.md
    <char>/hidden_truth.md
    <char>/voice.md
    <char>/state.json
  03_timeline/
    events.md
    open_loops.md
  04_outline/
    macro.md
    arc/
    scenes/
  05_chapters/
    ch001.md
    ch002.md
  06_packets/
    ch003_scene02.packet.json
  07_audit/
    contradictions.json
    ai_taste_report.json
  08_refs/
    samples/
    research/
```

运行流：

```text
Outline -> Scout -> Scene Packet -> Writer Draft -> Auditor -> Patch Rewriter -> Human Check -> State Updater
```

### scene packet 最关键

你真正要开发的核心，不是“大模型写作能力”，而是这个：

```json
{
  "scene_id": "ch03_s02",
  "pov": "顾临",
  "scene_goal": "让主角意识到师父在撒谎，但不能揭破真相",
  "must_include": ["药铺对话", "旧伤被看见"],
  "allowed_characters": ["顾临", "沈砚", "掌柜"],
  "allowed_lore": ["寒症规则", "药铺位置"],
  "forbidden_reveals": ["师父真实身份", "五年前事件真相"],
  "continuity_state": ["顾临尚未知道沈砚会武", "上一场争执未和解"],
  "style_rules": ["少解释", "对白带试探", "不升华总结"]
}
```

只要这个 packet 做好了，后面模型换成什么都还能跑。
这才是你做“CC/codex + LLM 写长篇小说工具”的真正护城河。

---

## 四、我建议你怎么借这些仓库，而不是照抄

最优策略不是选一个全盘继承，而是：

* **从 craft-companion 借流程骨架**
  渐进式披露、初始化引导、5 阶段流程。([GitHub][1])

* **从 inkos 借多 Agent 审核链**
  Writer / Auditor / Reviser 和人工门控。([GitHub][7])

* **从 稿匣 借本地优先与检索层设计**
  本地项目、可追溯执行轨迹、`FTS5 + embedding + rerank`。([GitHub][2])

* **从 ai_creator 借数据模型与 schema 化思路**
  World/Novel/Chapter 分层、共享 schema、工具化搜索和记忆。([GitHub][3])

* **从 AI_NovelGenerator 借 GUI 模块划分**
  设定工坊、状态跟踪、自动校对、工作台式界面。([GitHub][8])

---

## 五、最后给你的判断

你的方向是对的，而且你上传的汇总里最重要的洞见我基本认同：
**长篇小说 AI 工作流的本质，不是“让模型更会写”，而是“让系统只在该知道的时候知道该知道的东西”。** 

所以我给你的最终判断是：

**先做 Markdown-first 的刚性知识库，做 scene packet 编译器，做轻量审计链，做 benchmark harness。先别把时间砸在“大而全 RAG”或“全自动润色”上。**
这条路线最稳，也最容易真正做出不那么“AI 味”、能写长篇、还能持续迭代的系统。 ([GitHub][1])

下一步最值得做的，不是继续调研，而是先把 **目录结构 + scene packet schema + 3 条 benchmark 流程** 固定下来。

[1]: https://github.com/qcx1919788736-collab/craft-companion "GitHub - qcx1919788736-collab/craft-companion: AI协作小说创作框架 - 结构化知识库 + 双入口初始化 + 5阶段工作流 · GitHub"
[2]: https://github.com/mushroomfk/novel-generator "GitHub - mushroomfk/novel-generator: 稿匣：面向长篇小说创作的本地优先桌面工作台 · GitHub"
[3]: https://github.com/hackiey/ai_creator/blob/main/AGENTS.md "ai_creator/AGENTS.md at main · hackiey/ai_creator · GitHub"
[4]: https://github.com/Narcooo/inkos/blob/master/skills/SKILL.md?utm_source=chatgpt.com "inkos/skills/SKILL.md at master · Narcooo/inkos"
[5]: https://github.com/YILING0013/AI_NovelGenerator/blob/main/README.md?utm_source=chatgpt.com "AI_NovelGenerator/README.md at main"
[6]: https://arxiv.org/abs/2601.12740?utm_source=chatgpt.com "TreeWriter: AI-Assisted Hierarchical Planning and Writing for Long-Form Documents"
[7]: https://github.com/Narcooo/inkos "GitHub - Narcooo/inkos: Autonomous novel writing AI Agent — agents write, audit, and revise novels with human review gates · GitHub"
[8]: https://github.com/YILING0013/AI_NovelGenerator "GitHub - YILING0013/AI_NovelGenerator: 使用ai生成多章节的长篇小说，自动衔接上下文、伏笔 · GitHub"
***



你好！作为兼具AI工作流开发经验与长篇小说创作经验的“双栖”实践者，我非常理解你目前所处的阶段：**正在从“迷信神级提示词（Prompt）”向“构建严谨的系统工程”跨越。**

你提供的多份调研报告质量极高，它们指向了一个业界共识：**长篇小说创作绝不能依赖大模型的“一气呵成”，而必须通过“编排器（CC/Codex）+ 本地结构化文件 + 多Agent分阶段协作”的工程架构来实现。**

以下是我对这些调研报告的深度归纳与提炼，并针对你的四个核心疑问，给出**最务实、可落地的技术路线推荐**。

---

### 核心疑问解答与技术路线指引

#### 1. 小说信息管理：RAG vs Markdown，如何防止“剧透”（Plot Bleed）？
**结论：采用“混合架构”——Markdown管设定（人类可读），JSON管状态（机器核验），RAG管历史（按需追溯）。**

*   **RAG的局限**：纯RAG基于语义检索，找“黑袍法师”可能找不到“穿深色长袍的术士”，容易造成设定的逻辑断层。
*   **Markdown的优势与风险**：用Obsidian `.md` 文件管理角色确实是最符合直觉的，但把角色的“一生”全写在里面直接喂给AI，必然导致**信息提前披露（剧情渗漏）**。
*   **最推荐的解法（参考InkOS与Craft Companion）**：
    1.  **静态/基础设定（.md）**：存放世界观、角色基础档案（外貌、公开身份）。
    2.  **动态状态追踪（.json）**：建立核心的“真相文件（Truth Files）”，如`current_state.json`（当前位置/持有物）、`pending_hooks.json`（未收伏笔）。AI生成后由专门的Agent提取增量并更新JSON。
    3.  **信息隔离边界（防剧透机制）**：在角色档案中引入时间戳或章节锁。例如：`"隐藏身份: 魔王之子 [reveal_chapter: 30]"`。在写第15章时，系统脚本会在组装Prompt前，自动切除所有 `reveal_chapter > 15` 的字段。
    4.  **历史追溯（RAG/FTS）**：仅将**已经写完的历史章节**做向量化或全文索引（如稿匣的SQLite FTS5），用于跨章节的情节召回。

#### 2. 解决AI味过重：渐进式披露与Skill分级可行吗？
**结论：分级路由思路极其优秀，但不要把约束全部堆进LLM的Prompt里，要用“反向约束”+“独立审计”。**

*   **你的设想非常合理**：第一层选类型（写人），第二层选具体（暴躁铁匠），第三层选手法（短句白描）。这与 `ai_creator` 的 391+ Skill 系统不谋而合，能完美解决Prompt爆炸。
*   **潜在的“坑”**：如果你在第三层对大模型说“请优化这段文字，使其更加生动流畅”，这恰恰会触发AI的“默认平庸模式”，导致它疯狂使用“不禁”、“然而”、“不是...而是...”等陈词滥调。
*   **最推荐的解法**：
    1.  **L1与L2用代码逻辑跑**：类型和分类的匹配不要消耗Token，用代码标签（Tag）去本地库里精准拉取对应的 Skill 模板。
    2.  **L3建立《违禁风格指南》（Negative Constraints）**：不要教AI“怎么写得好”，而是用强硬的禁令告诉它“不能怎么写”。例如：禁用一切“瞳孔骤缩”、“倒吸一口凉气”等俗套生理反应；禁用“总而言之”等总结性过渡。
    3.  **提供风格锚点（Few-Shot）**：在注入手法Skill时，带上1-2段你自己写的、符合该手法的优秀人类文本片段，让AI进行“对齐”。

#### 3. 写作流程：一次成文 vs 写作+润色多Agent分离？
**结论：绝对要放弃“一次成文”。采用“章纲规划 -> 上下文编译 -> 盲写初稿 -> 独立审计 -> 定向修订”的流水线。**

*   **一次成文的灾难**：既要管剧情、顾设定，又要搞修辞、去AI味，这会导致LLM产生“认知过载”，最后剧情不仅错乱，文笔也全盘AI化。
*   **多Agent流水线（参考InkOS的写审分离）**：
    *   **Writer Agent（执行层）**：只拿本场景的细纲、当前有效设定和禁令库。任务是：**把剧情写对，别管文笔多华丽。**（可以使用Claude 3.5 Sonnet）
    *   **Auditor Agent（审计层）**：不修改文本！只输出JSON报告，检查剧情是否吃书、人物是否OOC、段落长度方差是否过小（AI味特征）。（可以使用更便宜的模型如 GPT-4o-mini 或 Haiku）
    *   **Reviser Agent（修订层）**：根据审计报告，**只对有问题的段落进行精准手术（Spot-fix）**，或者执行专门的去AI味Skill。
*   **Token消耗考量**：虽然总Token比一次成文多出约50%-80%，但一次成文一旦剧情崩坏，整章重写的成本是100%。多Agent分离实际上**大幅降低了人类返工的时间成本**，综合经济效益最高。

#### 4. 路线验证：用细纲做最小可行性测试（MVP）合适吗？
**结论：你的设想不仅合适，而且是所有成功开源项目的一致共识。**千万不要一上来就让AI“从零构思写完一本小说”。

*   **最佳验证步骤**：
    1.  **找“参照物”**：找一本你熟悉且文风有特色的已出版小说，人工提取其中连续 3-5 章的“精细细纲”（精确到每个场景的动作和对话意图）。
    2.  **验证“写对”**：跑通 `Writer Agent -> 真相文件提取`，看AI能不能在不剧透、不吃书的情况下把这3章顺下来。
    3.  **验证“写好”**：接入 `Auditor + Reviser`，看最后出来的文本，其“AI味检测”是否达标，能否逼近原作的文风。
    4.  **扩大规模**：上述跑通后，再开发并行生成、一键发布、大纲推演等长篇功能。

---

### 🌟 资深作者与架构师的最终技术路线推荐

为了配合你使用 **CC (Claude Code) / Codex 等本地工具**的优势，我为你量身定制以下工作流架构：

#### 架构定位
**把 CC/Codex 当作“交响乐团的指挥（编排器）”，而不是小说的直接作者。**让它利用本地文件读写能力来管理状态，将具体的写作任务分发给底层的 API (LLMs)。

#### 目录结构设计 (Obsidian + 机器双视角)
```text
/MyNovelProject
 ├── /01_Lore (世界观与人物设定 - Markdown，人类编辑)
 │    └── character_A.md (包含 [reveal_chapter: X] 标签)
 ├── /02_State (机器管理的真相文件 - JSON，CC维护)
 │    ├── current_status.json (当前位置、持有物品)
 │    └── pending_hooks.json (伏笔分类账本)
 ├── /03_Skills (技巧库，按场景类型分类的 .md 文件)
 ├── /04_Drafts (生成的正文草稿)
 └── workflow_controller.py / .sh (CC调用的主脚本)
```

#### 标准单章生成工作流（MVP核心）

1. **场景简报阶段（Context Builder - 本地代码执行）**
   * CC读取你要写的【本章细纲】。
   * CC执行脚本，扫描`/01_Lore`和`/02_State`，过滤掉未来的剧透信息，拼装成一个最小化的 `scene_brief.md`。
2. **初稿盲写阶段（Writer Agent）**
   * 触发大模型。输入：`scene_brief.md` + 选定的 `Skill`（只含该场景需要的手法和反向禁令词表）。
   * 输出本章初稿。
3. **状态提取与审计（Observer & Auditor Agent）**
   * Observer Agent 阅读初稿，提取出“男主得到了剑”、“两人到达了帝都”，CC将这些更新写入 `/02_State` 的 JSON 文件中。
   * 运行本地 NLP 脚本或轻量 LLM 审查AI味（比如统计长短句方差，检索是否误用了违禁词如“倒吸一口凉气”）。
4. **定向修订阶段（Reviser Agent）**
   * 如果审计不达标，CC把出问题的段落扔给 Reviser Agent，使用少样本提示（Few-shot）和“去除总结性发言”的指令进行重写。
5. **人类验收**
   * 在 Obsidian 中查看最终文本与状态更新，人工微调后，开始下一章。

### 结语

你现在的思考方向非常精准。**不要把时间花在寻找一个完美的、长达几万字的“一键写书Prompt”上，那是伪命题。** 
请立刻使用你的方案四（路线验证），手写3章细纲，借助 Claude Code 串联一个简单的 Python/Node 脚本读取本地 Markdown，接上 LLM API。只要你能让 AI 把这3章写得不脱离大纲、没有那股浓浓的“AI散文味”，你的长篇小说自动化工具就已经成功了80%！祝你开发和创作顺利！
***
基于对多个AI调研报告的归纳整理，结合您提出的四个核心疑问，我将从**小说信息管理、AI味控制、写作流程设计、路线验证**四个维度，为您系统分析各技术路线的优劣，并提供可落地的指导建议。

---

## 一、小说信息管理：RAG vs Markdown，如何兼顾效率与防剧透？

### 技术路线对比

| 维度 | RAG（向量检索+知识图谱） | Markdown（Obsidian等） |
|------|--------------------------|------------------------|
| **信息检索** | 语义检索，支持复杂关系查询、时间线查询，毫秒级响应 | 线性查找，依赖文件链接，速度较慢，易遗漏 |
| **一致性维护** | 自动校验冲突，通过知识图谱维护关系逻辑 | 人工维护，长篇易出错，设定冲突难发现 |
| **创作辅助** | 自动推荐相关设定、伏笔，智能提示 | 需手动查找，无智能辅助 |
| **版本控制** | 数据库事务+版本号，支持细粒度回滚 | 可用Git，但结构化信息管理粗糙 |
| **实现复杂度** | 高，需搭建向量数据库、图谱、检索逻辑 | 低，Obsidian原生支持，即开即用 |
| **信息披露风险** | 可设权限分级、动态加载、智能过滤 | 需手动控制引用，易误剧透 |

### 综合建议：**“RAG为主，Markdown为辅”的混合架构**

1. **核心角色/关键情节/伏笔线索**：用RAG管理。
   - 将角色设定、情节发展、伏笔线索结构化存储（如JSON/YAML），并向量化入库。
   - 通过知识图谱（如Neo4j）管理人物关系、时间线，支持复杂查询（如“主角敌人的敌人”）。
   - **防剧透机制**：
     - **权限分级**：为信息设置标签（如`公开`、`作者可见`、`编辑可见`），检索时过滤未到章节的内容。
     - **动态加载**：根据当前写作进度（章节号），只检索已发生情节的相关信息。
     - **智能过滤**：在Prompt中注入当前章节上下文，让RAG只返回当前视角可见的信息。

2. **次要角色/静态世界观/参考资料**：用Markdown管理。
   - Obsidian适合快速记录、双链引用，适合信息量小、无需复杂查询的内容。
   - **防剧透措施**：
     - 将Markdown文件按章节分目录，或使用`#剧透警告`标签手动隔离。
     - 在RAG检索时，将Markdown内容也向量化，但用标签控制可见性。

3. **工具集成思路**：
   - 以**Obsidian为前端**，编写Markdown文件，通过插件将结构化信息同步到RAG后端（如Milvus/Neo4j）。
   - 写作时，CLI/CC自动从RAG检索上下文，注入Prompt，同时保持Obsidian的编辑体验。

---

## 二、AI味控制：分层写作规范如何避免Prompt爆炸？

### 您的分级思路很好，但需注意以下问题：

1. **层级过深导致Prompt组装复杂**：
   - 若三层约束全注入Prompt，可能超出模型上下文长度，或干扰生成焦点。
   - **解决**：采用**“动态技能加载”**，根据当前写作场景只加载相关层级的约束。

2. **约束冲突**：
   - 例如“写反派”要求语言阴鸷，而“写景”要求宁静，若同时加载可能矛盾。
   - **解决**：设计**约束优先级机制**，场景约束 > 角色约束 > 通用约束。

3. **缺乏风格对齐**：
   - 仅约束写作手法，未约束语言风格（如网文口语化 vs 文学书面化），AI仍可能生成“翻译腔”。
   - **解决**：增加**“风格层”**，提供示例文本（如几段目标风格），让模型few-shot学习。

### 优化后的分层写作规范（四层）

| 层级 | 内容 | 示例 | 动态加载策略 |
|------|------|------|--------------|
| **L1 场景层** | 当前场景类型 | 对话/战斗/心理/写景 | 始终加载 |
| **L2 角色层** | 出场角色的性格、说话风格、当前状态 | 反派A：阴鸷、嘲讽；女主B：温柔但倔强 | 根据出场角色加载 |
| **L3 手法层** | 针对场景的写作手法 | 对话：少用“说”，用动作神态代替；写景：五感交织 | 根据场景类型加载 |
| **L4 风格层** | 整体语言风格、禁忌词、节奏 | 网文风：短句、口语、少形容词；禁用“值得注意的是” | 全局加载，但精简 |

### 实现方式：技能库 + 路由器

1. **技能库**：参考[ai_creator](https://github.com/hackiey/ai_creator)的391+技能，但按上述四层重组。
2. **路由器**：根据细纲中的场景标签、角色列表，动态组装Prompt：
   ```
   Prompt = L4风格层 + L1场景层 + L2角色层 + L3手法层 + 当前章节上下文
   ```
3. **Prompt压缩**：使用“指令-示例”格式，将长约束转化为几个典型示例，减少Token。

---

## 三、写作流程：一次成文 vs 多Agent流水线？

### 两种流程对比

| 维度 | 一次成文（单Agent） | 多Agent流水线（写+润） |
|------|---------------------|------------------------|
| **Token消耗** | 低（约1.5-2k tokens/千字） | 高（约2.5-3.5k tokens/千字） |
| **生成时间** | 快（3-5分钟/千字） | 慢（8-12分钟/千字） |
| **AI味控制** | 难，易模式化 | 易，润色Agent专攻去AI味 |
| **风格一致性** | 好，同一上下文生成 | 需协调，润色可能改变原意 |
| **可控性** | 低，后期改成本高 | 高，可分段审核 |
| **适用场景** | 短篇、初稿试写 | 长篇、商业作品 |

### 综合建议：**“粗写+精润”两阶段流水线**

1. **写作Agent**：
   - 任务：根据细纲+RAG上下文，生成章节初稿。
   - Prompt：重点注入L1-L3约束，要求故事完整、情节推进，不追求文笔。
   - 输出：粗稿，可能有AI味，但结构完整。

2. **润色Agent**：
   - 任务：去AI味、增强文笔、对齐风格。
   - Prompt：注入L4风格层+具体润色指令（如“删除虚词”“替换AI高频词”“增加动作描写”）。
   - 工具：可参考[去AI味提示词库](https://github.com/duongngo65yx-boop/De-AI-Prompt-Enhancer-Writer-Booster-SKILL)，内置AI味特征词黑名单。

3. **人工审核**：
   - 两阶段之间插入人工审核，确认情节无偏差，再送润色。
   - 润色后再次人工微调。

### 成本优化策略

- **长篇连载**：主线章节用流水线，过渡章节用一次成文。
- **模型选择**：写作Agent用快模型（如GPT-4o-mini），润色Agent用强模型（如Claude 3.5 Sonnet）。
- **批量润色**：积攒几章后统一润色，减少切换成本。

---

## 四、路线验证：如何从小闭环走向完整工作流？

### 您的设想合理，建议按以下步骤迭代：

#### 第一阶段：最小可行验证（1-2周）

1. **准备细纲**：从现有小说摘取1-2章细纲，或自己编写。
2. **搭建极简流程**：
   - 信息管理：用Obsidian记录2-3个角色设定（Markdown）。
   - 写作流程：单Agent一次成文，手动组装Prompt（L1-L3）。
   - AI味控制：手动修改，记录AI味高频词。
3. **验证目标**：
   - AI能否根据细纲生成完整章节？
   - 主要AI味问题有哪些？（列出TOP5）
4. **输出**：一份“AI味问题清单”和“可行基线版本”。

#### 第二阶段：引入RAG与润色（2-3周）

1. **实现简易RAG**：
   - 用FAISS+SQLite搭建本地向量库，将角色Markdown向量化。
   - 实现基础语义检索，按章节过滤信息。
2. **添加润色Agent**：
   - 根据问题清单，编写润色Prompt，实现自动去AI味。
3. **验证目标**：
   - RAG是否解决了“遗忘”问题？
   - 润色Agent能去除多少AI味？（对比前后AI检测分数）

#### 第三阶段：完善分层规范与多Agent协作（3-4周）

1. **重组技能库**：按四层分类，实现动态路由。
2. **实现写+润流水线**：用LangChain/LangGraph串联Agent。
3. **验证目标**：
   - 动态加载是否避免Prompt爆炸？
   - 流水线生成的章节是否达到“可读”水平？

#### 第四阶段：长篇压力测试（1-2周）

1. **测试5万字长篇**：验证一致性、防剧透、Token消耗。
2. **优化性能**：缓存常用RAG结果、压缩Prompt。
3. **用户测试**：邀请读者阅读，收集反馈。

### 关键验证指标

| 指标 | 合格标准 | 测量方法 |
|------|----------|----------|
| AI味 | AI特征词频率 < 5次/千字 | 用AI检测工具+人工统计 |
| 一致性 | 角色设定冲突 < 2处/5万字 | 人工检查关键角色属性 |
| 生成成本 | Token消耗 < 3k/千字 | 统计API调用 |
| 读者体验 | 阅读沉浸感评分 > 7/10 | 小范围读者调研 |

---

## 五、工具开发建议：基于开源项目的集成架构

### 推荐技术栈

| 模块 | 技术 | 理由 |
|------|------|------|
| 前端 | Obsidian + 插件 | 天然支持Markdown，双链，创作者熟悉 |
| 后端 | FastAPI + LangGraph | 轻量，易编排多Agent流程 |
| RAG | Milvus/FAISS + Neo4j | Milvus高性能向量库，Neo4j管理关系图谱 |
| 数据存储 | SQLite/PostgreSQL + YAML/JSON | 结构化数据用关系库，非结构化用文件 |
| 模型层 | 统一接口适配OpenAI/Anthropic/DeepSeek | 避免模型锁定 |

### 集成思路

1. **以[ai_creator](https://github.com/hackiey/ai_creator)的技能库为基础**，重组为四层技能体系。
2. **借鉴[NovelForge](https://github.com/zskaitocn/NovelForge)的动态输出模型**，确保章节生成格式一致。
3. **引入[文枢(WenShape)](https://github.com/unitagain/WenShape)的纯文本存储+Git版本控制**，方便回滚。
4. **自研Obsidian插件**，实现：
   - 一键同步设定到RAG。
   - 写作时自动注入RAG上下文。
   - 发起写作/润色Agent调用。

---

## 最终建议：开发路线图

```
第1-2周：MVP验证
  ├─ 用Obsidian+单Agent手写Prompt生成1章
  └─ 记录AI味问题

第3-4周：RAG与润色集成
  ├─ 搭建简易RAG（FAISS+SQLite）
  └─ 实现润色Agent

第5-6周：分层规范与流水线
  ├─ 重组技能库，实现动态路由
  └─ 串联写+润Agent

第7-8周：长篇测试与优化
  ├─ 5万字压力测试
  └─ 性能优化与读者测试
```

**核心原则**：从小闭环开始，每一步都验证效果，避免过度设计。AI写作工作流的关键不是技术复杂度，而是**人机协作的流畅度**——让AI承担重复劳动，让人聚焦创意与决策。
***